"""CSVAT — Celery stub (disabled for local Python run, no Redis required)."""

# Celery is NOT used in this local setup.
# Jobs run via FastAPI BackgroundTasks instead.

celery_app = None  # noqa — kept for import compatibility
